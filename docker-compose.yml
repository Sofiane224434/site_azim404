services:
  azim-site:
    build: .
    container_name: azim_main_site
    restart: unless-stopped
    ports:
      - "127.0.0.1:3001:80"
    networks:
      - web

networks:
  web:
    external: true

version: "3.8"

services:
  traefik:
    image: traefik:v2.10
    container_name: traefik
    restart: unless-stopped
    command:
      - "--api.insecure=true"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      - "--certificatesresolvers.myresolver.acme.httpchallenge=true"
      - "--certificatesresolvers.myresolver.acme.httpchallenge.entrypoint=web"
      - "--certificatesresolvers.myresolver.acme.email=admin@azim404.com"
      - "--certificatesresolvers.myresolver.acme.storage=/letsencrypt/acme.json"
      - "--entrypoints.web.http.redirections.entryPoint.to=websecure"
      - "--entrypoints.web.http.redirections.entryPoint.scheme=https"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
      - "./letsencrypt:/letsencrypt"
    networks:
      - web

  azim-site:
    build: .
    container_name: azim_main_site
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.azim.rule=Host(`azim404.com`) || Host(`www.azim404.com`)"
      - "traefik.http.routers.azim.entrypoints=websecure"
      - "traefik.http.routers.azim.tls.certresolver=myresolver"
      - "traefik.http.services.azim.loadbalancer.server.port=80"
    networks:
      - web

  portfolio-sofiane:
    build:
      context: ../portfolio
      dockerfile: Dockerfile
    container_name: portfolio_sofiane
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.portfolio.rule=Host(`sofiane-kherarfa.azim404.com`)"
      - "traefik.http.routers.portfolio.entrypoints=websecure"
      - "traefik.http.routers.portfolio.tls.certresolver=myresolver"
      - "traefik.http.services.portfolio.loadbalancer.server.port=80"
    networks:
      - web

networks:
  web:
    driver: bridge
